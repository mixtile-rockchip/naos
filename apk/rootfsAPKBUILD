pkgname=board-alpine-rootfs
pkgver=0.1
pkgrel=0
pkgdesc="board-alpine-rootfs"
url="focalcrest.com"
arch="all"
license="MIT"

source="board-alpine-rootfs.tar.gz"


build() {
        :
}

package() {
    mkdir -p "$pkgdir"/install
    install -Dm755 board-alpine-rootfs.tar.gz "$pkgdir"/install/board-alpine-rootfs.tar.gz
}

